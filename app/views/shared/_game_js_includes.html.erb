<!--Javascript that controls(?) trophies, settings etc-->

<link rel="stylesheet" type="text/css" href="/css/jquery-ui-1.8.18.custom.css">

<script type="text/javascript">

  var urls = {
    paurl: "<%= auth_pusher_index_path %>",
    settings_toggle_stats_url: "<%= settings_toggle_stats_url %>",
    settings_toggle_sounds_url: "<%= settings_toggle_sounds_url %>",
    settings_toggle_layout_url: "<%= settings_toggle_layout_url %>"
  };

  var badges = {
    gold: "<%= image_path "trophies/gold_trophy.png" %>",
    silver: "<%= image_path "trophies/silver_trophy.png" %>",
    bronze: "<%= image_path "trophies/bronze_trophy.png" %>",
    top10: "<%= image_path "trophies/top10_trophy.png" %>",
    top25: "<%= image_path "trophies/top25_trophy.png" %>",
    top100: "<%= image_path "trophies/top100_trophy.png" %>"
  }

  var ws_options = {
    is_production: <%= Rails.env.production? %>,
    pusherkey: "<%= Pusher.key %>"
  }

  head.ready(function() {

    game_page_init("<%= @game.name %>", "<%= @game.game_type %>", <%= @init_data.to_json.html_safe %>, <%= @maps.to_json.html_safe %>, <%= Rails.env.production? %>, "<%= Pusher.key %>", <%= current_user != nil ? current_user.id : 0 %>, "<%= current_user != nil ? current_user.username : "anon" %>", urls);

  });

</script>